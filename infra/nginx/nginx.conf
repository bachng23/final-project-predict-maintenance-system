events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    server {
        listen 80;
        server_name localhost;

        # --- CHUẨN BỊ CHO CÁC PHASE SAU ---
        
        # 1. Điều hướng Frontend (Next.js)
        # location / {
        #     proxy_pass http://frontend:3000;
        # }

        # 2. Điều hướng Web Backend (Express)
        # location /api/ {
        #     proxy_pass http://web_backend:4000;
        # }

        # 3. Điều hướng AI Backend (FastAPI)
        # location /ai/ {
        #     proxy_pass http://ai_backend:8000;
        # }

        # 4. Điều hướng WebSocket (Nginx cần Upgrade headers)
        # location /ws/ {
        #     proxy_pass http://web_backend:4000;
        #     proxy_http_version 1.1;
        #     proxy_set_header Upgrade $http_upgrade;
        #     proxy_set_header Connection "Upgrade";
        # }
    }
}